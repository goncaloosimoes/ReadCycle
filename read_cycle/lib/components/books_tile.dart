import 'package:flutter/material.dart';

class BooksTile extends StatelessWidget {
  const BooksTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}